# Keep this requirements list tight: only what the generated server imports directly.
# Transitives (starlette, anyio, etc.) come via FastAPI/Uvicorn.
fastapi==0.136.1
uvicorn[standard]==0.47.0
asyncpg==0.31.0
pydantic==2.13.4
typing-extensions==4.15.0
