# Local overrides for the two-node Besu chart.
# Private keys and derived public identities are injected by deployBesu().

validator:
  key: "set-from-contracts-env-at-deploy-time"

archive:
  key: "set-from-contracts-env-at-deploy-time"

rootDnsZone: "cbdc-sandbox.local"
