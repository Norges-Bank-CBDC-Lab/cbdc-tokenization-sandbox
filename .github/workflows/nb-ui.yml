name: NB UI - Format, Lint, Test, Build

on:
  pull_request:
    paths:
      - "services/nb-ui/**"
      - "package.json"
      - "package-lock.json"
      - "common/node-version.env"
      - ".github/workflows/nb-ui.yml"

permissions:
  contents: read

jobs:
  format-lint-test-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Load Node.js version
        id: node-version
        run: |
          set -a
          . common/node-version.env
          set +a
          echo "version=${SANDBOX_NODE_VERSION}" >> "$GITHUB_OUTPUT"

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "${{ steps.node-version.outputs.version }}"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install workspace dependencies
        run: npm ci

      - name: Run format check
        run: npm run format:check -w nb-ui

      - name: Run lint
        run: npm run lint -w nb-ui

      - name: Run unit tests
        run: npm test -w nb-ui

      - name: Run production build
        run: npm run build -w nb-ui
