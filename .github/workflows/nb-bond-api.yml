name: NB Bond API - Format, Lint, and Test

on:
  pull_request:
    paths:
      - "services/nb-bond-api/**"
      - "package.json"
      - "package-lock.json"
      - "common/node-version.env"
      - ".github/workflows/nb-bond-api.yml"

permissions:
  contents: read

jobs:
  format-lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Load Node.js version
        id: node-version
        run: |
          set -a
          . common/node-version.env
          set +a
          echo "version=${SANDBOX_NODE_VERSION}" >> "$GITHUB_OUTPUT"

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "${{ steps.node-version.outputs.version }}"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install workspace dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint -w nb-bond-api

      - name: Run format check
        run: npm run format:check -w nb-bond-api

      - name: Run unit tests
        run: npm test -w nb-bond-api
